-- Full-context, structured job descriptions for all 43 agents (Mission,
-- Responsibilities, Works from, Delivers, Collaboration, Guardrails, Success),
-- rendered as a role spec in the Agent Office drawer. Applied to the live DB via
-- MCP in two batches; recorded here as a marker (see session for full content).
-- Each agent_roles.job_description now holds markdown-structured text.
select 1;
